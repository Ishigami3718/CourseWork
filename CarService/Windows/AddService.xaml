<Window x:Class="CarService.Windows.Window2"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CarService.Windows"
        mc:Ignorable="d"
        Title="Window1" Height="300" Width="500">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.3*"/>
            <RowDefinition/>
            <RowDefinition Height="0.3*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <TextBlock Margin="5" VerticalAlignment="Center" HorizontalAlignment="Center" Text="Сервіс" FontSize="18"/>
        <TextBlock Grid.Column="1" Margin="5" VerticalAlignment="Center" HorizontalAlignment="Center" Text="Деталі на складі" FontSize="18"/>
        <TextBlock Grid.Column="2" Margin="5" VerticalAlignment="Center" HorizontalAlignment="Center" Text="Деталі для сервісу" FontSize="18"/>
        <ListBox Grid.Row="1"  Margin="5" ItemsSource="{Binding Services}"/>
        <ListBox Grid.Row="1" Grid.Column="1" Margin="5" ItemsSource="{Binding Details}"/>
        <ListBox Grid.Row="1" Grid.Column="2" Margin="5"/>
        <Button Grid.Row="2" Margin="10" Width="100" Content="Обрати"/>
        <Button Grid.Row="2" Grid.Column="1" Margin="10" Width="100" Content="Додати"/>
        <Button Grid.Row="2" Grid.Column="2" Margin="10" Width="100" Content="Зберегти"/>
    </Grid>
</Window>
